
<%= provide(:title, @title) %>
<% if @persons.any? %>
<table>
  <tr>
    <th>First Name</th>
    <th>Last Name</th>
  </tr>
	<% @persons.each do |p| %>
    <tr>
      <td><%= p.first_name %></td>
      <td><%= p.last_name %></td>
    </tr>
  	<% end %>
</table>
<% end %>
